name: Build release asset

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build single-file harness
        run: make single-file
      - name: Upload asset to release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/harness
